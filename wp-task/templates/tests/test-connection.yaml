apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wp-task.fullname" . }}-test-connection"
  labels:
    {{- include "wp-task.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wp-task.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
